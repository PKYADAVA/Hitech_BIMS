# Base URL of the Django /api/v1 backend.
# Simulator/emulator can use localhost; a PHYSICAL DEVICE must use your
# machine's LAN IP (and that IP must be in Django's ALLOWED_HOSTS).
#   EXPO_PUBLIC_API_BASE_URL=http://192.168.1.20:8000/api/v1
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
