node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Local env overrides
.env
.env.local

# Native build output (managed workflow regenerates these)
/ios
/android

# Release signing. Losing the keystore means never updating the Play listing
# again, so this is backed up out of band, not committed.
/credentials
